# Εγκατάσταση σε απομονωμένο τοπικό δίκτυο

Το πακέτο περιέχει την εφαρμογή, Bootstrap, Font Awesome, Python 3.11 και όλα τα απαιτούμενα Python packages. Δεν απαιτείται Internet ούτε κατά την εγκατάσταση ούτε κατά τη λειτουργία.

## Υπολογιστής-server

1. Αντιγράψτε ολόκληρο τον φάκελο του project στον υπολογιστή που θα λειτουργεί ως server.
2. Ανοίξτε PowerShell μέσα στον φάκελο.
3. Εκτελέστε:

```powershell
powershell -ExecutionPolicy Bypass -File .\install_offline.ps1
```

Ο εγκαταστάτης ελέγχει πρώτα SHA-256 hashes για τον Python installer, όλα τα wheels και όλα τα τοπικά frontend assets. Αν κάποιο αρχείο λείπει ή έχει αλλοιωθεί, η εγκατάσταση σταματά.

4. Για πρόσβαση από άλλους υπολογιστές, ανοίξτε PowerShell ως Διαχειριστής και εκτελέστε μία φορά:

```powershell
powershell -ExecutionPolicy Bypass -File .\configure_lan_firewall.ps1
```

Ο προεπιλεγμένος κανόνας επιτρέπει τη θύρα 5000 μόνο σε δίκτυα `Domain` και `Private`. Αν το απομονωμένο δίκτυο έχει χαρακτηριστεί `Public`, ο διαχειριστής δικτύου πρέπει πρώτα να εγκρίνει την αλλαγή ή να εκτελέσει το script με `-Profile Public`.

5. Ξεκινήστε την εφαρμογή:

```powershell
powershell -ExecutionPolicy Bypass -File .\start_offline.ps1
```

Το παράθυρο εμφανίζει τις διαθέσιμες τοπικές διευθύνσεις, π.χ. `http://192.168.1.20:5000`.

## Υπόλοιποι υπολογιστές

Δεν εγκαθιστούν τίποτα. Ανοίγουν έναν browser και πληκτρολογούν τη διεύθυνση LAN που εμφανίζεται στον server. Όλα τα δεδομένα και τα αρχεία παραμένουν στον server.

## Σημαντικά

- Ο server πρέπει να έχει σταθερή IP ή κράτηση DHCP.
- Μην ενεργοποιείτε Flask debug mode στο κοινό δίκτυο.
- Δημιουργείτε συχνά backup με την εντολή `flask backup-db` ή από προγραμματισμένη εργασία.
- Κρατήστε αντίγραφο του φακέλου `backups` σε δεύτερο προστατευμένο μέσο.
- Η SQLite είναι κατάλληλη για μικρή μονάδα και ελαφριά ταυτόχρονη χρήση. Για μεγάλο αριθμό ταυτόχρονων εγγραφών χρειάζεται τοπικός PostgreSQL server.
